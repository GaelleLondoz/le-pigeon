import React, { Fragment, useEffect, compon } from 'react'

const ProfileItem = () => {
    return (
        <div>

        </div>
    )
}
axios.get(`users/${id}`).then(res => {
    this.setState({ path: res.path })
})
export default ProfileItem
